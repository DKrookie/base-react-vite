import Logo from '@/components/Logo';
import type { SettingObject } from '@/types';

const setting: SettingObject = {
  layoutHeaderHeight: 64,
  siderCollapsedWidth: 40,
  logo: Logo,
  logoText: 'practice',
};

export default setting;
